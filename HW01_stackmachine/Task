Требуется реализовать интерпретатор стековой машины (той, что была на занятии). Интерпретатор получает на вход текст программы для машины (каждая инструкция на отдельной строке, пустые строки и лишние пробелы игнорируются, разрешены метки) и исполняет её, читая входной поток с входного потока и пиша выходной поток в выходной.

С n - загрузить константу n на вершину стека
L x - со стека в переменную Х
S x - из переменной Х в стек
R - с входной ленты на стек
W - со стека на выходную ленту
B o - арифметическая операция с двумя числами из стека, результат на верхушку {+-*/% == != > < >= <= || &&}
J a - переход на позицию а в программе
JT а - только если на вершине стека не 0 (True)
JF a - аналогично для false
E - остановка
